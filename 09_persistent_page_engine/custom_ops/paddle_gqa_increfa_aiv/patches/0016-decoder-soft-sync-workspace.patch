diff --git a/attention/paddle_gqa_incre_flash_attention_aiv/op_host/incre_flash_attention_tiling.cpp b/attention/paddle_gqa_incre_flash_attention_aiv/op_host/incre_flash_attention_tiling.cpp
--- a/attention/paddle_gqa_incre_flash_attention_aiv/op_host/incre_flash_attention_tiling.cpp
+++ b/attention/paddle_gqa_incre_flash_attention_aiv/op_host/incre_flash_attention_tiling.cpp
@@ -2874,6 +2874,11 @@ ge::graphStatus IFATiling::CalcWorkSpace()
         }
     }
 
+    // The fused Paddle B1 decoder entry uses the first 32 bytes per launched
+    // AIV as the public software-SyncAll flag array. The kernel shifts the raw
+    // workspace pointer by the same amount before entering stock attention.
+    workspaceSize_ += static_cast<size_t>(16U) * static_cast<size_t>(32U);
+
     if (ifaContext_->workSpaces) {
         ifaContext_->workSpaces[0] = workspaceSize_;
     }
