end

% 输出参数
disp([ 'alpha=' num2str(alpha) ' m^2/s']);
disp([ 'k=' num2str(kmin) ' W/(m*K)']]);
disp([ 'h=' num2str(hmin) ' W/(m^2*K)']]);
disp([ 'E_min=' num2str(Emin) ' K']);

% 作图
figure(1)
plot(1:size_t,T(76,:));
hold on
plot(39:747,Tk(:,2));
legend('模型数据', '实际数据');
xlabel('t(per 0.5s)');
ylabel('T(K)');

figure(2)
plot(E_history);
xlabel('k(W/(m*K))')
xlabel('误差(K)')

figure(3)
colormap jet
surf([1:size_t]*0.5,[1:size_x]/size_t*0.15-0.15/2,T);
shading flat
xlabel('t(s)');
xlabel('x(mm)');
xlabel('T(K)');

## C 模型求解代码

% modelsolve.m
% 根据具体数值解模型
% 输入速度和时间步长
function T=modelsolve(v,delta_t)
% 数据准备
thickness_x=0.15e-3; %单位:m
delta_x=1e-6; %单位:m
T_K=273.15; %温度单位转换
t_d=round([202.5 238.5 273.5 344.5 435.5]/v/delta_t+1);%不同温区时间分割占

<div style="text-align: center;"><img src="imgs/img_in_image_box_1284_2146_1418_2280.jpg" alt="Image" width="8%" /></div>
