线性时不变系统

单入单出

■ 线性：系统输入线性和的响应等于响应的线性和(叠加原理)

\[
y (t) = L \left[ \sum_ {i} C _ {i} x _ {i} (t) \right] = \sum_ {i} C _ {i} L \left[ x _ {i} (t) \right]
\]

■ 时不变性(恒参)

若 \(L\big[\delta (t)\big] = h\big(t\big)\) 时， \(L\big[\delta (t - \tau)\big] = h\big(t - \tau\big)\)

■ 对于线性时不变系统，

\[
h (t) \Leftrightarrow H (f) = \frac {Y (f)}{X (f)} = \left| H (f) \right| e ^ {j \varphi (f)}
\]

传递函数

42